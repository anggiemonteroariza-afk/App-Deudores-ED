import streamlit as st
import pandas as pd
import os
from datetime import datetime

# ---------------------------------------------------------
# CONFIGURACIÓN DE LA PÁGINA
# ---------------------------------------------------------
st.set_page_config(page_title="Mini App Deudores", page_icon="💸")

st.title("💸 Mini App de Registro de Deudores")
st.write("Aplicación sencilla para registrar, consultar y actualizar cartera.")


# ---------------------------------------------------------
# CARGA DE BASE DE DATOS
# ---------------------------------------------------------
FILE_PATH = "DeudoresPrueba.xlsx"

# Si existe el archivo, lo carga; si no, crea uno nuevo
if os.path.exists(FILE_PATH):
    df = pd.read_excel(FILE_PATH)
else:
    df = pd.DataFrame(columns=[
        "ID", "Cliente", "Documento", "Fecha", "Valor", "Estado", "Observaciones"
    ])

# ---------------------------------------------------------
# GUARDAR CAMBIOS
# ---------------------------------------------------------
def save_df(dataframe):
    dataframe.to_excel(FILE_PATH, index=False)


# ---------------------------------------------------------
# MENÚ LATERAL
# ---------------------------------------------------------
menu = st.sidebar.selectbox(
    "Selecciona una opción",
    ["Registrar Deudor", "Consultar Deudores", "Editar Registro"]
)

# ---------------------------------------------------------
# OPCIÓN 1: REGISTRAR DEUDOR
# ---------------------------------------------------------
if menu == "Registrar Deudor":
    st.subheader("➕ Registr


